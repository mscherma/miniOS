
BOOT_2ND_STAGE_SEG	EQU	0x6000
BOOT_2ND_STAGE_OFF	EQU	0x0000

GDT_OFFSET		EQU	0x0400

SIZE_2ND_STAGE		EQU	0x4000

KERNEL_SIZE		EQU	0x800
KERNEL_ENTRY_OFFSET	EQU	0x00400000

SECTOR_SIZE		EQU	0x200
TOTAL_KERN_SIZE		EQU	(SIZE_2ND_STAGE + KERNEL_SIZE) / SECTOR_SIZE

MEMORY_MAP_ENTRY_SIZE	EQU	0x18
SIZE_MAX_MEMORY_MAP	EQU	(0x20 * MEMORY_MAP_ENTRY_SIZE)
